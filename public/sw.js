// A basic service worker for PWA installability
self.addEventListener('fetch', (event) => {
  // This service worker is intentionally left blank for now.
  // It's just here to make the app installable.
  // You can add caching strategies or other PWA features later.
});
